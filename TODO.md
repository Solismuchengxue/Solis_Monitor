# Solis Monitor 任务清单

本文件只维护当前任务、阻塞、实施顺序和验收条件。稳定需求与架构放在
`DESIGN.md` 和 `docs/`；仓库审计见 `docs/REPOSITORY_REVIEW.md`；已经完成的
演进由 Git 历史保存，踩坑过程放在本地忽略的 `DEVLOG.md`。

## 维护规则

- 新问题或新需求先写入本文件，再开始实现。
- 每项任务使用稳定编号，并写明范围、影响和可观察的完成标准。
- 同一时间原则上只推进一个当前任务，避免多条功能线同时扩散。
- `[ ]` 待处理，`[~]` 进行中，`[!]` 阻塞，`[x]` 已验证完成。
- 完成后只保留简短结果和验证证据；详细过程进入 Git 历史或 `DEVLOG.md`。
- 完成项定期移出本文件，避免再次变成历史流水账。

## 当前基线

- 基线分支：`main`；当前实施分支：`codex/background-collection-reliability`
- 当前可用版本：PC `0.9.6`，固件 `0.1.5`
- 当前功能基线：PC 指标、Codex 指标、天气、DHT11、AP/STA WebUI、6 位码配对、
  设备控制、本地 OTA、通知和安装包已形成可用闭环。
- 当前任务：`RUNTIME-002` 后台采集可靠性加固
- 详细审计和判断依据：[`docs/REPOSITORY_REVIEW.md`](docs/REPOSITORY_REVIEW.md)

## 当前里程碑：后台采集可靠性加固

### RUNTIME-002 隔离后台采集异常并建立有界日志

状态：`[~]`

范围：

- 隔离指标与天气后台采集中的可恢复异常，保留最后一次完整有效值；
- 建立脱敏、限流且有 512 KiB 硬上限的本地运行时错误日志；
- 补强天气诊断恢复、Codex 会话元数据类型边界和运行时回归测试；
- 完成全量验证和仓库内发布载荷核验后，再单独申请部署、推送和 Release 授权。

实施计划：
[`docs/superpowers/plans/2026-08-08-background-collection-reliability.md`](docs/superpowers/plans/2026-08-08-background-collection-reliability.md)

当前进度（2026-08-08）：

- `[x]` Task 1：增加后台采集异常保护器，提交 `b79a2de`；
- `[x]` Task 2：增加有界脱敏运行时错误日志，提交 `11052e1`；
- `[x]` Task 3：接入指标与天气定时器及诊断恢复，提交 `aa43289`，127/127 冒烟测试通过；
- `[x]` Task 4：补齐 Codex 会话元数据 JSON 类型边界，提交 `18a4c4b`，128/128 冒烟测试通过；
- `[ ]` Task 5：记录后台恢复、日志路径、脱敏和容量边界；
- `[ ]` Task 6：执行全量验证、依赖漏洞审计并核验仓库内发布载荷。

当前边界：Task 1–4 仅位于实施分支，尚未合并 `main`、推送、部署或更新 GitHub Release。

## 当前里程碑：仓库收敛

### CLEAN-001 清理本地生成物

状态：`[x]`

范围：

- 保留或另行归档最终安装包
  `build/installer/SolisMonitor-0.9.6-win-x64-setup.exe`；
- 保留或另行归档正式固件
  `build/release/solis_monitor-0.1.4.bin`；
- 删除其他 `build/`、`build_pairing/`、`firmware/build*`、
  `firmware/test_apps/unit/build*`、`app/**/bin,obj`；
- 删除 `firmware/sdkconfig.old`、`.superpowers/` 和确认无用途的空目录；
- 保留 `.venv/`、`.codegraph/`、`.vscode/` 和 `DEVLOG.md`。

完成标准：

- 最终 PC 安装包和正式固件仍可找到；
- 预计释放约 3.09 GB；
- `git status --short` 只显示预期的受控文件变更；
- Python 仓库结构测试仍通过。

完成结果：2026-07-26，删除 56 个已校验目标、20593 个生成文件，实际释放约
3.09 GB。最终安装包和 `0.1.4` 固件仍保留在原路径，SHA-256 分别为
`2684f5d18da331cf37ef006e2bf3c271dccabdb5372e32538344e765c18eabbd` 和
`72fb831ef5d54636084e9a72cedd2146cad0cf90a52d67d8c94c7d8f27381f23`。
Python 测试 10/10 通过，Git 仅显示本轮文档变更。

注意：两个交付文件位于被忽略的 `build/` 中，执行 `git clean -fdX` 前必须先另行
归档，否则会一并删除。

### CLEAN-002 删除明确过时的受控内容

状态：`[x]`

范围：

- 删除未被运行入口调用的 `FirstRunOnboardingForm`；
- 删除永久返回“不显示”的 `FirstRunOnboardingPolicy`；
- 删除对应的三条无意义冒烟测试；
- 删除无调用者的 `MainForm.SelectableNetworkAdapters`；
- 删除未使用的 `MaoKenZhuYuanTi-MaokenZhuyuanTi-2.ttf`；
- 删除不会被当前仓库 GitHub 识别的
  `app/LibreHardwareMonitor/.github/`。

完成标准：

- CodeGraph 和文本搜索不再出现悬挂引用；
- PC 冒烟测试全部通过；
- .NET 10 x64 Release 构建通过；
- 不改变设备向导、自动网卡选择和开发者模式行为。

完成结果：2026-07-26，删除旧首次向导和策略、对应测试、无调用者属性、未使用
字体及嵌套上游 `.github/`。CodeGraph 同步后不再存在相关符号引用；单并发
.NET 10 x64 Release 构建为 0 警告、0 错误，剩余 63 项冒烟测试全部通过。
验证生成的 `bin/obj` 已再次清理。

### CLEAN-003 完成文档收敛

状态：`[x]`

范围：

- 将全局审计固定到 `docs/REPOSITORY_REVIEW.md`；
- 将本文件收敛为当前任务台账；
- 修正 README、DESIGN 和专题文档中已经不存在的 `docs/plans/`、历史计划及旧任务编号；
- 保留稳定需求、协议、硬件、UI、天气和迁移专题文档。

完成标准：

- 根目录仍只保留 README、DESIGN、TODO 和本地 DEVLOG 四个 Markdown 入口；
- 文档不存在失效的相对链接和已删除目录描述；
- TODO 有足够空间记录后续任务，不再保存大段历史验收流水。

完成结果：2026-07-26，新增 `docs/REPOSITORY_REVIEW.md` 固化全局审计，将 TODO
从 537 行、193 个已完成项收敛为当前任务台账，并同步修正 README、DESIGN、
DESKTOP_APP、TESTING、REQUIREMENTS 和 DECISIONS 的旧目录及历史任务描述。

## 后续重构

这些任务必须在死代码和本地产物清理完成后分别实施，不与普通清理混合。

### REFACTOR-001 拆分 PC 控制中心页面

状态：`[x]`

- 将 `SolisControlCenterControl.cs` 按设备、服务、启动、固件、开发者页面和公共 UI
  原语拆分；
- 保持现有布局、主题、DPI、设备控制和页面导航行为；
- 由现有冒烟测试及人工页面检查保护。

完成结果：2026-07-26，将设备、服务、启动、固件和开发者页面及各自事件处理器
移动到五个 `SolisControlCenterControl.*.cs` partial 文件。核心文件从 1691 行
降至 1024 行，页面逻辑未改；CodeGraph 已识别新的文件边界，单并发 .NET 10 x64
Release 构建为 0 警告、0 错误，63 项冒烟测试全部通过。验证生成物已再次清理。

### REFACTOR-002 拆分 PC 冒烟测试入口

状态：`[x]`

- 按 Codex、天气、设备、OTA、启动、诊断和 UI 分文件；
- 不删除测试场景，不降低覆盖要求；
- 保持单命令执行和明确失败输出。

完成结果：2026-07-26，将本轮拆分前 2466 行的 `Program.cs` 收敛为 1 行入口，
并按 UI、设备、启动、诊断、固件、指标、协议、Codex 和天气拆为 partial 测试文件；公共
断言和模拟处理器集中到共享文件，63 个测试的注册顺序和失败输出保持不变。
CodeGraph 已同步 13 个变更文件；单并发 .NET 10 x64 Release 构建为 0 警告、
0 错误，63/63 冒烟测试通过。

### REFACTOR-003 拆分固件配网门户

状态：`[x]`

- 从 `provisioning_portal.c` 分离门户生命周期、配对接口和设备控制/OTA 接口；
- 保持 AP、STA WebUI、配对码、恢复默认设置和 OTA 协议不变；
- 完成固件单元测试、正式构建和适用实机验收。

当前结果：2026-07-26，核心门户从 1049 行降至 412 行；配对接口、设备控制与
OTA 分别移动到独立源文件，门户状态和内部处理器声明集中到私有内部头。CodeGraph
已同步；ESP-IDF 6.0.2 正式固件和 Unity 单元测试镜像均完成编译与链接，正式镜像
占最小 OTA 槽 87%、剩余 13%，单元测试镜像剩余 32%。Python 测试 13/13 通过。
正式重构镜像已通过局域网 OTA 写入真实副屏；局域网 WebUI、6 位码发现与退出、
AP 配网页面与退出、设备信息、令牌认证、显示控制原值回写、远程重启和 OTA 状态
均完成实机验收。远程重启实测 0.5 秒后离线、2.5 秒后重新上线，AP 验收后原
Wi-Fi 和配对关系保持不变。

Unity 测试通过默认关闭的 `SOLIS_UNIT_OTA_COMPAT` 构建开关生成 OTA 测试镜像，
避开不稳定的 COM4 写入链路；实机运行结果为 78 Tests、0 Failures、0 Ignored。
测试镜像没有确认 OTA 槽，正常断电重启后 bootloader 自动回滚到正式 0.1.4；
设备网络、配对和回滚保护均确认恢复。

### REFACTOR-004 收敛 MainForm 的 Solis 协调职责

状态：`[x]`

现状证据：

- `MainForm.cs` 约 1930 行、143 个符号，CodeGraph 影响范围 274 个符号；
- 主窗体直接持有指标快照、Codex、天气、网络、设备令牌、Device API、设备发现、
  离线/令牌失配通知、设备控制和 OTA 共 18 个 Solis 字段；
- 两个后台采集定时器的创建、回调和释放，以及服务启动/停止顺序均在主窗体中；
- 控制中心构造函数需要主窗体转发 20 余个状态读取和操作委托。

拟实施边界：

1. 新增非 UI 的 `SolisRuntime` 协调对象，统一持有 Solis 采集器、快照、Device API、
   设备发现、诊断、通知状态、设备控制和 OTA 客户端；
2. 由 `SolisRuntime.Start()` / `Dispose()` 统一管理启动和反向释放顺序，并确保释放后
   不再发生后台回调；
3. `MainForm` 继续持有 LibreHardwareMonitor `Computer`、传感器树、硬件快照映射、
   WinForms 控件、托盘通知呈现、设备向导弹窗和开发者模式；
4. 硬件后台更新只把 `MappedHardwareMetrics` 写入运行时；后台通知由运行时产生结果，
   主窗体只负责切回 UI 线程并显示；
5. 第一轮不改变控制中心页面、不修改 Device API/配对/OTA 协议，也不收窄上游
   LibreHardwareMonitor 功能。

实施前必须先补的回归保护：

- `[x]` 运行时启动和释放可重复调用，关闭后不再发布指标或通知；
- `[x]` 设备扫描取消后等待在途扫描退出，再释放 `HttpClient`、信号量和取消令牌；
- `[x]` Device API 授权事件在释放前解除订阅；
- `[x]` 天气配置切换和测试后正确释放内部 `HttpClient`；
- `[x]` 原有测试与新增生命周期测试、.NET 10 x64 Release 构建保持通过。

建议分两次最小变更：

- 第一批只修正并测试设备扫描、定时器、授权事件和天气客户端的生命周期；
- 第二批在这些测试保护下引入 `SolisRuntime`，替换 `MainForm` 中对应字段和委托转发。

第一批结果：2026-07-26，`DeviceDiscoveryService` 已改为跟踪并等待当前扫描任务，
释放可重复调用；`QWeatherMetricsCollector` 只释放自己创建的 `HttpClient`，天气测试
和配置切换会及时释放旧实例；主窗体关闭前解除 Device API 授权事件，并用关闭状态
阻止迟到的 Codex/天气回调继续发布结果。单并发 .NET 10 x64 Release 构建为 0 警告、
0 错误，65/65 冒烟测试通过。定时器与服务的统一可等待停止仍属于第二批
`SolisRuntime` 范围。

第二批结果：2026-07-26，新增非 UI 的 `SolisRuntime`，统一持有指标快照、Codex/
天气/网络采集、Device API、设备令牌、发现、诊断、通知状态、设备控制、OTA 和两个
采集定时器。`MainForm` 中对应的 17 个后台字段收敛为一个 `_solisRuntime`，文件由
约 1930 行降为 1809 行；硬件树映射、托盘呈现、向导和确认框仍留在 UI。新增运行时
回归测试使用临时配置、临时 Codex 目录和随机本地端口，验证重复启动、释放后停止
发布、重复释放和释放后拒绝重启。net472 与 .NET 10 x64 Release 构建均为 0 警告、
0 错误，66/66 冒烟测试通过。新发布输入的 79 个文件已逐项校验并覆盖
`D:\Solis Monitor`，3 个用户配置文件哈希未变化；真实管理员进程启动后，18472
鉴权请求返回 schema 1，序列号在 2 秒内从 52 增至 54，副屏仍在线且已配对。设备、
服务、启动与托盘、固件更新页面均通过人工验收，托盘正常退出成功。
REFACTOR-004 已关闭。

详细判断依据见 `docs/REPOSITORY_REVIEW.md` 的 MainForm 专项复审。

## 当前里程碑：体验修复与控制中心视觉重构

### WEATHER-001 收敛天气位置名称

状态：`[x]`

- 中国大陆地区只显示“省·市”，例如“辽宁·大连”，不再显示区县；
- PC 天气测试结果、Device API 与副屏使用同一名称；
- 更新对应天气回归测试。

完成标准：坐标解析仍使用区县级位置 ID 获取天气，但所有用户界面只显示省和市。

完成结果：2026-07-26，位置 ID 与天气查询精度保持不变，用户可见名称统一为
“省·市”。桌面端 66/66 冒烟测试、Python 16/16 测试和正式固件构建通过。

### SERVICE-001 修复 PC 服务重启反馈

状态：`[x]`

- “服务 → 重启服务”显示执行中、成功或失败结果；
- 不再丢弃 Device API 重启返回值；
- 失败时保留可诊断的错误提示。

完成标准：用户点击后能明确看到操作结果，Device API 实际恢复监听并通过状态检查。

实现进度：按钮现会显示“正在重启”，并根据实际返回值提示成功或失败；桌面端构建和
冒烟测试通过。仍需在正式运行程序中人工点击一次，确认本机 18472 监听恢复。

完成结果：2026-07-28，正式运行程序中人工点击“重启服务”，成功提示“设备 API
已重新启动，端口 18472 正在监听”，服务链路恢复正常。

### SENSOR-001 恢复真实硬盘温度

状态：`[x]`

- 继续使用真实 NVMe 当前温度，不把 Warning/Critical Temperature 阈值当成当前温度；
- 优先复用 LibreHardwareMonitor 上游与 DiskInfoToolkit 的新存储实现；
- 依赖升级和上游代码迁移需经确认后执行。

完成标准：两块物理 NVMe 均显示与 DiskInfo 工具一致或合理接近的当前温度，且阈值不会
误显示为实时温度。

完成结果：2026-07-26，已按 LibreHardwareMonitor 2026-07-24 上游存储实现迁移到
DiskInfoToolkit 2.1.2；管理员探针与正式 Device API 均确认 Intel VMD/RST 下能够
读取两块 NVMe 的实时温度。最终部署后 Lexar SSD NM710 1TB 为 47°C、ZHITAI
TiPlus7100 2TB 为 39°C，Warning/Critical Temperature 阈值未被当作实时温度。
桌面端 Release 构建 0 警告、0 错误，66/66 冒烟测试通过。

### DISPLAY-001 统一副屏温度文本

状态：`[x]`

- 内存温度去掉 `TEMP`；
- PC、GPU、显存、内存、硬盘和室内温度统一使用 `°C`；
- 保持 800×480 页面现有信息不溢出。

完成标准：所有副屏温度值使用 `°C`，内存行只保留占用率与温度值。

实现进度：全部温度文本已改为 `°C`，内存行已移除 `TEMP`；字体资源重新生成，正式
固件和 Unity 单元测试镜像均构建通过。仍需 OTA 后检查 800×480 实机排版。

完成结果：2026-07-28，副屏固件 0.1.5 实机确认所有温度均使用 `°C`，内存行不再
显示 `TEMP`，周使用 Token 正常显示为“亿”，800×480 页面未发现溢出。

### DISPLAY-002 重做夜间背光设置交互

状态：`[x]`

- PC 设备页改为按钮打开设置弹窗，弹窗中设置启用状态和起止时间；
- 只有点击保存后才写入副屏，取消不改变现有配置；
- 保存成功后副屏显示“夜间背光设置成功”和配置时间，随后自动返回原页面。

完成标准：PC 明确反馈保存结果，副屏确认页不改变当前页面选择和其他显示设置。

实现进度：PC 弹窗、单次保存和副屏 5 秒确认页已完成；亮度单独调整不会误触发确认。
桌面端起止时间已改用 Windows 自带 `DateTimePicker`，支持跨午夜范围且不再需要
手工校验文本格式；相关回归测试通过。桌面端、正式固件和 Unity 单元测试镜像均构建
通过。2026-07-27 修复
`MaskedTextBox` 将 `07:00` 中的零误当占位符、导致跨午夜时段保存失败的问题；
新增真实弹窗回归测试，`23:59–07:00` 现可通过校验。仍待部署后进行 PC/副屏联机
验收。

完成结果：2026-07-28，PC 设备页实机设置 `23:59–07:00` 成功；副屏正确显示
夜间背光设置成功及配置时间，并在约 5 秒后返回原页面，当前页面选择和其他显示设置
保持不变。

### UI-001 重构 PC 控制中心视觉与服务总览

状态：`[x]`

- 保留 WinForms、LibreHardwareMonitor 后端和现有功能入口；
- 按 [`docs/PC_UI_DESIGN.md`](docs/PC_UI_DESIGN.md) 建立统一的导航、卡片、状态、
  间距、图标、输入控件、按钮和弹窗规范；
- 重做设备、服务、启动与托盘、固件更新和开发者模式全部页面；
- 重做设备向导、配对码、天气、夜间背光、恢复默认设置及 Solis 通用结果弹窗；
- 参考 Clash Verge 的信息层级和总览感，不照搬其产品内容。

实现进度：页面外壳、五个页面、主要业务弹窗和统一结果弹窗的源码改造已完成；
开发者模式沿用的上游窗口已统一主题。PC Release 构建和冒烟测试已通过，待真实窗口
截图与参考设计并排验收。

完成标准：所有页面和 Solis 自有弹窗使用同一设计系统；100%、125%、150% DPI
无重叠和裁切；系统文件选择器与 UAC 保持原生；业务行为不变；最终用真实窗口截图
与已确认参考图并排验收。

实现进度：三套方向已确认采用“服务依赖链”方案；服务页已重排为整体摘要、
PC 后台 → Device API → 副屏链路、Codex/天气独立采集以及修复与导出区，保留天气
编辑、重启服务、复制诊断和恢复默认设置。单并发 Release 构建为 0 警告、0 错误，
66/66 冒烟测试通过；最终发布包已部署到 `D:\Solis Monitor` 并通过 81 个文件的
SHA-256 清单校验。Computer Use 实机检查确认主要卡片无文字截断；同时发现页面进入
时可能保留旧滚动位置导致标题裁切，已补充进入页面时复位滚动位置并再次通过 Release
构建与 66/66 冒烟测试。最终修复已重新部署到 `D:\Solis Monitor`，81 个文件通过
SHA-256 清单校验，Device API 正常启动。用户复查认为实际视觉仍与设计稿相差较大，
第二轮已改为三张独立状态卡、圆角渐变卡片、服务链连接关系、Segoe Fluent Icons、
带图标导航和圆角按钮，并再次通过 Release 构建与 66/66 冒烟测试。Computer Use
检查部署后的第二轮界面后，第三轮进一步补齐品牌图标、收敛未选中导航状态，压缩
首屏纵向密度，减弱嵌套卡片边框并强化服务链箭头；Release 构建仍为 0 警告、
0 错误，66/66 冒烟测试通过。第三轮已部署到 `D:\Solis Monitor`，81 个文件通过
SHA-256 清单校验；Computer Use 在 1107×841 实际窗口中确认品牌区、导航、摘要卡、
服务依赖链和独立采集区均正常显示，核心全局状态已进入首屏，“修复与导出”因窗口
高度仍位于下方滚动区。第四轮开始逐页统一：设备、启动与托盘、固件更新及开发者页
复用的基础卡片已切换为服务页同款圆角渐变容器，启动说明也纳入统一卡片层级；固件
页移除了已经放弃的 GitHub Release 占位入口。单并发 Release 构建为 0 警告、
0 错误，66/66 冒烟测试通过。第四轮发布包已部署到 `D:\Solis Monitor`，81 个文件
全部通过 SHA-256 校验。用户复查确认这些轮次仍属于旧布局的局部换肤，没有形成统一
组件与弹窗体系；2026-07-26 已按确认参考图重新建立完整设计规范，进入“共享组件 →
全页面 → 全弹窗”的整体重做阶段。

当前状态（2026-07-27）：仍未达到参考设计和用户验收标准。最近几轮混用 WinForms
与嵌入式 WPF 后出现过切页黑块、绘制延迟、布局越界和点击区域错位；这些现象不能
视为单纯配色问题。后续必须先固定单一页面承载和绘制路径，再按同一窗口尺寸逐页与
参考图对照，禁止在未经截图验收时标记完成。设备 API 与开机启动故障已另由
`RUNTIME-001` 修复，不再与视觉重构混在一起。

最新进度（2026-07-27）：已确认此前完整 WPF 控制中心从未接入运行入口，实际仍由
WinForms 页面承载，并在服务页额外嵌入一个 WPF 子页，这是双侧栏、黑块和命中错位的
结构根因。现改为完整 WPF 控制中心作为唯一可见页面层；旧 WinForms 控件只保留为
兼容状态层，不再参与绘制和布局。设备、服务、启动与托盘、固件更新四页均完成
1100×800 离屏渲染，未发现黑块、裁切、残影或布局越界。最终视觉基准已更正：服务页
不再使用旧稿的三张摘要卡，改为页头状态与时间、三节点服务链路、两条外部采集信息和
右下操作按钮；当时的同尺寸并排对照确认主要结构已经对齐。Release 构建
为 0 警告、0 错误。2026-07-27 最新一轮恢复设备 API 卡片的“本机与局域网服务”
说明，将端口独立显示为“端口 18472”，修复 Codex/天气图标裁切，并按实际 DPI
缩小底部两个操作按钮；外部采集图标改为独立的圆形绘制槽，最新同尺寸离屏渲染未见
裁切。x64 冒烟测试 68/68 通过。
真实 `ElementHost` 连续切页与
125%/150% DPI 仍待部署后人工验收，因此状态保持 `[~]`。

回归补强（2026-07-28）：新增 WPF 控制中心连续切页测试，在 STA 布局线程中以
1100×800 视口连续完成 20 轮、共 80 次设备/服务/启动/固件页面切换；每次均重新布局并
确认只有当前页可见、实际尺寸有效。针对从 125% 切回 100% 后窗口逻辑高度增加、
设备页底部卡片被纵向拉伸的问题，设备详情区现固定为 500 DIP，并新增
1426×993 大视口回归检查。桌面端 71/71 冒烟测试通过；2026-07-28 已在 100% 缩放下
实机复查四个页面，未发现重叠、裁切、黑块或异常拉伸。125% 和 150% 仍保留为人工
视觉验收项。
相关一次性视觉验收记录和截图已于 2026-07-31 经用户授权清理；完成结论由本任务后续
记录继续保留。

2026-07-30 原生 WPF 默认入口回归（待修复）：

- 设备页仅显示设备状态，亮度、夜间背光、清除配对和远程重启没有接入运行时操作；
- 服务页诊断快照未随运行时刷新，Device API 长期停在“正在启动”、副屏长期停在
  “正在扫描”，重启服务没有可观察结果；
- 服务链与外部采集详情重复或叠字，天气设置弹窗在当前尺寸下裁切；
- 版本号未注入，连续点击版本号无法解锁开发者入口；
- 原生 WPF 托盘菜单遗漏“显示副屏 WebUI”。

本轮完成标准：上述入口均由原生 WPF 宿主直接连接 `SolisRuntime`，操作有成功或失败
反馈；诊断状态定时刷新；版本与开发者入口恢复；托盘 WebUI 仅在已发现设备时可用；
相关回归测试、单并发 Release 构建和真实设备人工验收全部通过。

完成结果：2026-07-30，设备、服务、启动与托盘、固件更新及开发者模式页面已统一到
原生 WPF 控制中心；运行时操作、诊断刷新、版本入口、托盘 WebUI 和业务弹窗均恢复。
用户完成多轮真实窗口验收后，将当前视觉评价为约 80 分，并明确剩余小瑕疵不值得继续
优化。PC `0.9.6` 稳定基线的 Release 构建为 0 警告、0 错误，桌面冒烟测试全部通过。
后续若重新提出具体视觉问题，再建立独立任务，不继续扩大本项范围。

下一步最小验收顺序：

1. 先稳定设备页的尺寸、绘制和命中区域，连续切换四个页面不得出现黑块或旧页面残影；
2. 再统一服务、启动与托盘、固件更新页的共享卡片、按钮、状态和滚动行为；
3. 最后统一所有 Solis 自有弹窗，并在相同 DPI、相同窗口尺寸下保存逐页对照截图；
4. 每一步均需通过 Release 构建、冒烟测试和真实窗口人工验收后再继续。

### RUNTIME-001 修复启动路径与管理员权限故障

状态：`[x]`

问题与修复：

- 使用 `dotnet SolisMonitor.dll` 启动时，依赖检查错误地以宿主可执行文件目录为基准，
  导致明明存在 `Aga.Controls.dll` 仍提示缺失；现改用 `AppContext.BaseDirectory`；
- 使用 `RunAsInvoker` 启动的临时 UI 观察实例没有管理员权限，导致 Device API
  18472 未监听，并在切换开机启动时触发 `E_ACCESSDENIED`；
- `StartupManager` 的初始化路径会检查管理员权限，但属性写入路径只检查计划任务
  服务连接状态，判断不一致；现统一为“管理员且计划任务服务可用”才写最高权限任务，
  否则使用当前用户启动项分支。

验证结果：2026-07-27，.NET 10 x64 Release 构建为 0 警告、0 错误；DLL 宿主和
应用宿主两种启动路径均不再误报依赖缺失；正常管理员实例监听 18472；计划任务
`SolisMonitor` 状态为 Ready，执行目标、`--windows-startup` 参数和工作目录均指向
`D:\Solis Monitor`。

### PERF-001 收敛 PC 端常驻 UI 与内存

状态：`[x]`

已确认事实：

- WPF 控制中心初始化成功后会立即结束旧控制中心构造流程，正常路径只创建一个
  `ElementHost`，不会同时创建隐藏的 WinForms 控制中心页面；
- LibreHardwareMonitor 原始传感器树、菜单和 Sensor Gadget 仍由 `MainForm` 在启动时
  创建；绘图面板和绘图窗体已改为仅在进入开发者模式时按需创建；
- 2026-07-29 当前正式进程基线约为工作集 307 MB、私有工作集 156 MB、
  Private Bytes 210 MB、约 900 个句柄；低于此前观察到的约 1.5 GB，但尚未达到
  100 MB 目标；
- 2026-07-29 已将传感器实时历史窗口默认关闭，并移除主程序中的 24 小时历史窗口
  初始化与传播逻辑；当前值和最小值/最大值仍保留，新增回归测试确认历史集合为空。
  该修改已通过 79 项桌面冒烟测试，83 个发布文件已按 SHA-256 清单覆盖到
  `D:\Solis Monitor`。新版冷启动约为工作集 285 MB、Private Bytes 199 MB；
  稳定后 30 秒采样保持在工作集 288–289 MB、Private Bytes 200–201 MB，没有
  继续线性增长。与修改前约 307/210 MB 的基线相比有所下降，但主要常驻占用显然
  不在实时历史集合；
- 同一正式版本最小化后 30 秒仍约为工作集 294 MB、Private Bytes 202 MB，说明窗口
  最小化不会释放主要常驻资源；开发者绘图懒加载已通过 80 项桌面冒烟测试，尚待发布
  后重新测量冷启动、显示、最小化和首次进入开发者模式的差值；
- 绘图懒加载版本已按 83 项 SHA-256 清单部署。启动后 5/15/35 秒分别约为
  288/290/293 MB 工作集、201/203/203 MB Private Bytes，和上一版基本相同，
  说明 OxyPlot 对象不是主要常驻占用；
- 2026-07-29 使用微软官方 `dotnet-gcdump` 对管理员权限正式进程采样：GC 堆为
  28,221,828 字节、371,818 个对象，采样后进程约为工作集 294 MB、Private Bytes
  199 MB。最大单项是约 4.6 MB 的 `System.Byte[]`，没有发现足以解释 200–300 MB
  常驻内存的托管对象集合；
- 堆内同时存在 `MainForm`、`Aga.Controls.Tree.TreeViewAdv`、WPF 控制中心和 WPF
  页面实例，但这些托管对象本身只占很小部分。当前约 170 MB Private Bytes 位于 GC
  堆之外，可能来自 CLR/JIT、WinForms/WPF 图形栈、原生硬件访问库和驱动映射；现有
  快照不能把这 170 MB 精确归因给其中某一项；
- 因此停止继续微调历史集合和单个控件对象。下一步应做可回退的宿主架构对照实验，
  量化“当前 WinForms 主窗体承载 WPF”与“WPF 主窗体直接承载后台服务”的差值，再
  决定是否正式移除旧 WinForms 开发者界面。
- 2026-07-30 已把默认程序入口切换为原生 WPF `Application`，并将旧
  `MainForm` 收窄为只有显式 `--legacy-ui` 才会进入的回退路径；第二实例唤醒和
  服务页请求通过 WPF Dispatcher 封送。新增 2 项回归后，100 项桌面冒烟测试全部
  通过，Release 构建为 0 警告、0 错误；随后已完成人工功能验收和正式进程内存
  复测。旧 WinForms 源码和依赖仍作为显式回退保留。
- 2026-07-30 新增只读取既有进程的 `tools/Measure-SolisMonitorMemory.ps1`，
  统一采样工作集、Private Bytes、句柄数和线程数；脚本不会启动、结束或修改
  Solis Monitor，可用于后续显示态、托盘态和长时运行的同口径复测。
- 2026-07-30 对已运行的原生 WPF 正式进程完成 60 秒短时采样：工作集
  242.8 MB、Private Bytes 134.4 MB，起止均无变化；线程数保持 17，句柄数由
  907 增至 915。该结果说明短样本内没有持续内存增长，但不能替代托盘态和
  30–60 分钟长时采样。
- 2026-07-30 对同一原生 WPF 正式进程完成 30 分钟托盘态采样（61 个样本）：
  工作集由 243.0 MB 变为 243.1 MB，范围 242.8–244.7 MB；Private Bytes 起止均为
  134.6 MB，范围 134.3–136.3 MB；线程数范围 16–21，起止均为 18；句柄数范围
  805–921，起止为 821/865，但样本中持续上下波动而非单向增长。该样本没有发现
  内存持续增长迹象，当前问题是约 243 MB 的稳定常驻开销，不是已复现的内存泄漏。
- 2026-07-31 对同一进程完成控制台显示态对照（21 个样本、20.2 秒）：工作集稳定为
  246.6 MB，Private Bytes 为 145.9–146.0 MB，线程数 17–19，句柄数 828–939。
  与托盘态长期均值相比，显示窗口约增加 3.3 MB 工作集和 11.2 MB Private Bytes；
  可见窗口本身不是约 243 MB 常驻开销的主要来源。该短样本只用于状态差值，不用于
  判断长期泄漏。
- 启动链复核确认：默认入口直接进入 `SolisWpfDesktopHost`，只有显式
  `--legacy-ui` 才会创建旧 `MainForm`，现有冒烟测试同时覆盖选择器和启动器；
  但托盘静默启动仍会预先构造完整 WPF 窗口和页面。将窗口改为首次显示时才创建
  需要拆分 presenter/view 生命周期，影响第二实例唤醒、托盘和设备状态刷新；显示态
  对照只能量出渲染增量，不能量出“完全不构造 WPF 页面”的收益，因此不作为低风险
  优化直接实施。

实施顺序：

1. `[x]` 区分托管堆与非托管常驻开销：托管堆约 28 MB，不能解释当前约 199 MB
   Private Bytes；
2. `[x]` 测量原生 WPF 默认入口在控制台显示、最小化托盘及长时运行后的工作集、
   Private Bytes、句柄数和增长量；已完成 30 分钟托盘态样本与 20 秒显示态差值对照；
3. `[x]` 保留 `--legacy-ui` 回退，同时将默认入口切换为不创建 `MainForm`、
   `ElementHost`、旧传感器树和旧 WinForms 托盘的原生 WPF 宿主；
4. `[x]` 完成原生 WPF 正式进程的显示态、最小化托盘态和长时运行对照；本轮没有
   修改运行代码，因此不重复执行冷启动或开发者模式增量测试；
5. `[x]` 默认原生 WPF 入口此前已通过 100 项桌面冒烟测试和人工功能验收；本轮
   文档收口不重新构建或覆盖正式安装。

完成标准：正常用户路径不常驻仅供旧开发者界面使用的控件和绘图资源；功能与开发者
入口保持可用；以实测数据报告节省量，不在缺少剖析证据时承诺低于 100 MB。

完成结果：2026-07-31，默认入口已是原生 WPF，旧 WinForms 仅保留为显式
`--legacy-ui` 回退；30 分钟托盘态没有发现持续内存增长，显示窗口相对托盘态平均只
增加约 3.3 MB 工作集和 11.2 MB Private Bytes。当前约 243 MB 工作集属于稳定常驻
开销，现有证据不足以证明继续拆分 WPF 生命周期能带来与风险相称的收益，因此本项
按“完成测量、保留回退、不承诺低于 100 MB”的标准收口。

## 待决策事项

### BUILD-001 收敛桌面构建矩阵

状态：`[x]`

已确认边界：

- Solis Monitor 主程序只支持 `.NET 10 x64`；
- `Aga.Controls` 保留上游 `net472` 和原架构声明；
- `LibreHardwareMonitorLib` 保留上游多目标框架和多架构声明。

完成结果：2026-07-26，主程序项目收敛为 `net10.0-windows`、`x64` 和
`win-x64`，移除仅服务旧主程序目标的条件引用，并清理 .NET 10 已内置的两个直接
包引用。主程序和冒烟测试均为 0 警告、0 错误，66/66 冒烟测试通过；正式发布输入
包含 79 个文件，清单框架和架构为 `.NET 10 x64`。上游两个项目的矩阵未改变。

### TOOL-001 处理 Pillow 弃用接口

状态：`[x]`

- 将资源生成脚本中的 `Image.getdata()` 迁移到 Pillow 推荐接口；
- 在 Pillow 14 发布前完成；
- 保持 RGB565/RGBA565 输出完全一致并通过资源可复现测试。

完成结果：2026-07-26，按 Pillow 官方迁移建议将两处像素遍历改为
`get_flattened_data()`，并增加弃用警告回归测试。Python 测试 12/12 通过，
修改前后 36 个生成文件的 SHA-256 完全一致。

## 持续风险与约束

- Codex 会话 JSONL 是桌面版内部格式，没有稳定公开契约。
- LibreHardwareMonitor 传感器名称可能随硬件、驱动和语言变化。
- 显存温度、内存温度和 FPS 并非所有硬件或场景都能提供。
- HTTP 设备 API 依赖 Windows 防火墙、管理员权限和可信局域网，目前不提供传输加密。
- ESP32 只有 2 MB PSRAM，增加大图、缓存或双缓冲前必须核算内存。
- 天气 API Key、Wi-Fi 密码和完整设备令牌不得写入源码、日志或 Git。
- 完整 .NET/ESP-IDF 构建可能产生大量缓存和较高 CPU 占用，应按任务需要执行，
  不在纯文档或只读审计阶段重复运行。

## 新任务模板（复制后填写）

### TYPE-NNN 任务名称

状态：`[ ]`（模板，当前没有创建该任务）

范围：

- 要修改的内容；
- 明确不修改的边界。

完成标准：

- 自动验证；
- 需要时的实机验收；
- 文档和 Git 状态检查。
