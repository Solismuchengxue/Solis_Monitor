# Solis Monitor 设计文档

> 状态：当前设计基线
> 更新日期：2026-07-26
> 适用范围：ESP32-S3 固件、Windows 桌面端、PC 与设备通信协议、小屏界面和配套工具

## 1. 文档目的

本文是 Solis Monitor 的设计总入口，用于统一记录：

- 项目目标、边界和需求基线；
- 硬件、固件、桌面端和通信协议的总体设计；
- Codex 指标的采集原理、隐私边界和兼容风险；
- 稳定的需求基线、组件边界和关键设计决策；
- 项目应维护的专题文档及其职责。

本文同时描述目标架构和必要的当前实现约束。编号化、可验收的稳定需求见 [`docs/REQUIREMENTS.md`](docs/REQUIREMENTS.md)，重大架构决策与替代方案见 [`docs/DECISIONS.md`](docs/DECISIONS.md)；任务状态、缺陷、环境阻塞和实施顺序统一维护在 [`TODO.md`](TODO.md)，不在本文重复维护。

## 2. 项目概述

Solis Monitor 是一套由 Windows 桌面端和 ESP32-S3 小屏设备组成的电脑状态副屏：

- Windows 桌面端基于 Libre Hardware Monitor，采集电脑硬件、网络和 Codex 使用情况；
- 桌面端通过可信局域网内的 HTTP 设备 API 发布统一指标快照；
- ESP32-S3 每秒轮询指标，在 3.97 英寸 800×480 横屏上显示电脑状态和 Codex 状态；
- DHT11 温湿度由 ESP32 本地读取，不绕行 PC；
- 天气由 PC 获取后随设备指标下发；
- 设备支持 AP Web 配网，同时核心指标链路继续使用局域网 HTTP 轮询，不依赖门户常驻。

### 2.1 核心目标

1. 在 800×480 小屏上清晰显示高价值指标，不堆砌完整传感器树。
2. 复用 Libre Hardware Monitor 的成熟硬件访问能力，不重新实现硬件监控内核。
3. 使用轻量、可控的自绘界面，不引入 LVGL。
4. 明确区分有效值、不可用值、陈旧值和传输离线状态。
5. 跟随 Codex 桌面版最后活动的主任务，显示项目、上下文和两类周额度。
6. 保留可诊断性、可测试性和后续维护空间，但不为未确认需求预建复杂框架。
7. 将 Windows 程序定位为后台采集服务、托盘程序和单设备控制中心，不复制小屏数据看板。

### 2.2 非目标

- 不把 ESP32 做成完整的 Libre Hardware Monitor 客户端或传感器浏览器；
- 不使用 WebSocket；设备继续通过 HTTP 轮询；
- 不在 ESP32 上采集 Windows 硬件指标；
- 不通过 PC 采集 DHT11；
- 不恢复旧独立 Agent、旧串口 JSON 协议或 LVGL 工程；
- 不在可信局域网方案中自行实现 HTTPS 证书体系。
- 不在普通桌面界面重复展示 CPU、GPU、内存、硬盘、网络、Codex 和天气看板；
- 不提供历史趋势、Codex 用量管理或日常传感器筛选功能。

## 3. 硬件基线

详细引脚、电气约束和来源见 `docs/HARDWARE.md`。

| 模块 | 设计基线 |
| --- | --- |
| 主控 | ESP32-S3-WROOM-1-N8R2 |
| Flash | 8 MB Quad SPI Flash |
| PSRAM | 2 MB Quad SPI PSRAM |
| 显示屏 | 3.97 英寸 IPS，物理分辨率 480×800，横屏逻辑分辨率 800×480 |
| 显示控制器 | NT35510，16 位 I80，RGB565 |
| USB 转串口 | CH340C，UART0 日志与下载 |
| 温湿度 | DHT11，GPIO47 |
| 用户按键 | GPIO21，低电平有效，用于页面切换 |
| 背光 | GPIO38，硬件支持 PWM，当前按开关使用 |

关键约束：

- LCD DB0 使用 GPIO0，复位期间不得被外部持续拉低；
- GPIO19、GPIO20 已用于 LCD，不使用 ESP32-S3 原生 USB；
- PSRAM 必须配置为 Quad SPI；
- 完整 RGB565 帧缓冲占 768,000 字节，必须从 PSRAM 分配；
- DHT11 不高频采样，读取失败必须表现为不可用而不是 0。

## 4. 总体架构

```text
┌──────────────────────────────── Windows ────────────────────────────────┐
│                                                                         │
│  Libre Hardware Monitor ──┐                                             │
│  Windows 网络计数器 ──────┼─> SolisRuntime ─> 各采集器与最新读数缓存    │
│  Codex 本地会话文件 ──────┤                         │                   │
│  和风天气 API ───────────┘                         ▼                   │
│                                      单一 1 Hz 快照发布器                │
│                                                   │                     │
│                                                   ▼                     │
│                                      HTTP :18472 /api/v1/metrics        │
│                                      Bearer Token，schema 1             │
└───────────────────────────────────────────────────┬─────────────────────┘
                                                    │ 可信局域网，每秒 GET
┌──────────────────────────────── ESP32-S3 ─────────▼─────────────────────┐
│  Wi-Fi/NVS 配置 → HTTP 客户端 → JSON 协议解析 → Dashboard Store         │
│                                                        │                │
│  DHT11 本地采集 ────────────────────────────────────────┤                │
│                                                        ▼                │
│                                  UI 状态 → 局部重绘 → NT35510           │
│                                                                         │
│                                  GPIO21：电脑页 / Codex 环境页切换      │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.1 设计原则

- 采集器只负责产生带时间戳和可用状态的读数；
- 单一发布器每秒组装一次完整、不可变快照；
- UI 和 HTTP 读取同一份快照，不各自触发采集；
- 设备端解析必须是事务性的：完整消息有效才替换当前状态；
- 传输在线状态与单字段 availability 分开；
- 扩展字段应保持 schema 兼容，破坏性协议变更才提升 schema 版本。

## 5. 目录和职责

```text
Solis_Monitor/
├─ app/                         Windows 桌面端及其测试
│  ├─ LibreHardwareMonitor/     上游 LHM 源码和 Solis 扩展
│  │  └─ LibreHardwareMonitor/
│  │     ├─ Program.cs          桌面程序入口
│  │     ├─ UI/                 原始窗口、托盘和交互
│  │     └─ Solis/              Solis 业务层
│  │        ├─ SolisRuntime.cs   后台服务、采集定时器与资源生命周期
│  │        ├─ Codex/           Codex 本地指标采集
│  │        ├─ DeviceApi/       ESP32 HTTP API
│  │        ├─ Hardware/        LHM 传感器映射
│  │        ├─ Metrics/         统一指标模型和快照
│  │        ├─ Network/         出口网卡与网速
│  │        ├─ Security/        设备令牌
│  │        └─ Firmware/        本地固件校验和局域网 OTA
│  └─ tests/                    PC 端冒烟测试
├─ firmware/                    ESP-IDF 工程
│  ├─ main/app_main.c           固件入口
│  ├─ components/               板级、显示、网络、协议、UI、OTA 等组件
│  ├─ test_apps/unit/           ESP-IDF 单元测试应用
│  ├─ sdkconfig.defaults        构建配置基线
│  ├─ partitions.csv            8 MB 双 OTA 分区表
│  └─ version.txt               正式固件版本
├─ docs/                        项目专题文档
│  ├─ HARDWARE.md               硬件清单、引脚和电气约束
│  ├─ LEGACY_REFERENCE.md       旧实现中提炼的硬件验证参数
│  ├─ DESKTOP_APP.md            PC 端构建、运行和设备 API
│  ├─ FIRMWARE.md               固件启动、分区、构建和 OTA
│  ├─ PROTOCOL.md               PC 与副屏的 HTTP 协议
│  ├─ PROVISIONING.md           AP 配网、发现和配对
│  ├─ TESTING.md                自动构建与实机验收
│  ├─ PC_MIGRATION.md           PC 端迁移历史和兼容边界
│  └─ REPOSITORY_REVIEW.md      仓库收敛审计和整理依据
├─ reference/                   原理图、屏幕厂商资料和原始素材
│  └─ assets/                   图标、字体、背景和天气素材
├─ tools/                       资源生成和统一验证脚本
├─ README.md                    项目简介和快速开始
├─ DESIGN.md                    本设计总入口
├─ TODO.md                      唯一任务和状态台账
└─ DEVLOG.md                    本地维护日志，Git 忽略
```

## 6. 功能需求

### 6.1 电脑指标

| 模块 | 指标 | 数据来源 | 不可用行为 |
| --- | --- | --- | --- |
| CPU | 占用率、温度、主频、功耗 | LHM 进程内对象模型 | 单字段显示 `--` |
| GPU | 占用率、核心温度、核心主频、功耗 | LHM，默认优先独立显卡 | 单字段显示 `--` |
| GPU 显存 | 占用率、已用、总量、显存温度 | LHM，按硬件支持情况 | 不以 0 代替缺失值 |
| 内存 | 已用、总量、占用率、温度 | LHM；温度依赖实际传感器 | 单字段缺失显示 `--` |
| 存储 | 各物理硬盘名称、占用率、温度 | LHM；屏幕当前同时容纳最多 4 块 | 单字段缺失显示 `--` |
| 网络 | 当前出口网卡上传、下载速度 | Windows 网络累计计数器 | 首样本和异常间隔不可用 |
| FPS | 帧率 | LHM 可用 FPS 传感器或以后确认的数据源 | 无真实来源时显示 `--` |

桌面端应允许选择首选 GPU、NVMe 和网卡；无有效选择时使用可解释的自动选择规则。小屏只显示一组上下行速度，不展示全部网络设备。

### 6.2 Codex 指标

目标小屏需要显示：

- 项目名：会话 `cwd` 的最后一级目录名；
- 任务名：会话索引中的 `thread_name`，空间允许时显示；
- 模型和推理强度：最后活动主任务最新 `turn_context`；
- 上下文已用量，单位 K；
- 上下文窗口总量，单位 K；
- 上下文占用百分比；
- 主周额度的友好名称、剩余百分比和重置时间；
- GPT-5.3-Codex-Spark 周额度的友好名称、剩余百分比和重置时间；
- 最后活动主任务累计 Token 数；
- Codex 活跃/不活跃状态。

当前实现已把项目名、任务名、模型和推理强度作为独立字段写入统一快照与设备协议。小屏上下文卡优先显示项目、模型和推理强度；任务名保留在协议中供桌面端与后续诊断使用，不与项目名混用。

选择规则：

1. 只考虑主任务，排除 `source.subagent` 会话；
2. 当前阶段以会话文件最后写入时间判断最后活动任务；
3. 上下文跟随最后活动任务切换；
4. 周额度从所有会话的最新额度事件中选择，不能因切换项目回退到旧读数；
5. 后台出现时间更新的额度事件时允许额度恢复，以适应非固定重置时间；
6. 连续 10 分钟没有新的有效上下文计数事件时标记为“不活跃”，并保留最后有效值。

上述项目名、任务名和友好额度名称均为目标显示语义；当前字段映射以第 8 节所述实现为准。

### 6.3 天气和室内环境

- 天气由 PC 通过和风天气 API Key 认证获取：地点、天气描述、室外最低温、室外最高温、风向、风力等级和图标码；
- 天气设置提供 API Host、API Key、经度和纬度；中国大陆使用 GCJ-02，其他地区使用 WGS84，查询链接只保留高德坐标拾取器；
- API Key 默认隐藏，使用当前 Windows 用户保护的方式保存；界面只显示“已配置”，更换时重新输入，不提供明文查看或复制；
- DHT11 由 ESP32 本地每 2 秒读取：室内温度、湿度；本地快照拥有这两个字段，PC 指标刷新不得覆盖；
- 天气失败不影响电脑指标，DHT11 失败不影响 PC 通信；
- 每个环境字段拥有独立 availability；
- 环境卡片不能将尚未采集的值显示为 0。

### 6.4 配置与管理

- 没有有效配置时固件自动进入 AP 配网；已有配置时长按 GPIO21 约 5 秒进入，短按仍切换页面；
- 配网期间使用 AP+STA 模式但暂停 STA 和 PC 指标请求，AP 固定为 `192.168.0.1/24`，避免与现有局域网同网段时发生路由冲突；
- 通配 DNS 和 HTTP 404 重定向引导到 gzip 单页门户；门户只提供 Wi‑Fi 扫描、SSID、密码、保存连接和恢复默认设置；
- 门户每次 HTTP 活动刷新 10 分钟计时；保存成功后关闭 AP 并按双槽 NVS 中的新配置重连，超时则恢复进入配网前的有效 STA 配置；
- Windows IPv4、固定端口和设备令牌由 6 位码配对流程自动交换，不允许在 WebUI 中查看或手工编辑；
- AP 与 STA 复用同一页面；可信家庭局域网内可直接修改 Wi‑Fi，恢复默认设置经页面二次确认后清除网络与配对数据并重启进入 AP；
- 普通 STA 掉线只执行退避重连，不自动开放 AP；
- 串口 `setup`、`show`、`reconnect`、`clear` 保留为救援入口；
- 配置存储在 NVS，日志不得输出 Wi-Fi 密码或完整设备令牌；
- 桌面端自动发现同一局域网内的唯一小屏；自动发现失败时通过折叠的救援入口手动填写设备 IP；
- 首次配对和令牌失配恢复必须双击 GPIO21 开启发现并输入 6 位配对码；令牌由桌面端自动生成并同步，普通界面不允许查看或复制；
- 桌面端保留 LHM 原始功能，但普通界面只提供设备控制、配置、故障处理和更新；
- 开发者模式显示完整传感器树、诊断信息和模拟数据入口；
- 程序支持托盘、静默启动和管理员开机启动；关闭与最小化窗口固定隐藏到托盘，真正退出只通过托盘菜单完成。

## 7. 通信协议设计

### 7.1 传输

| 项目 | 设计 |
| --- | --- |
| 协议 | HTTP/1.1 |
| 地址 | `http://<PC-IP>:18472/api/v1/metrics` |
| 方法 | GET |
| 认证 | `Authorization: Bearer <64 个十六进制字符的令牌>` |
| 刷新 | ESP32 每秒请求一次 |
| 最大响应 | 4096 字节 |
| 缓存 | `Cache-Control: no-store` |
| 使用范围 | 可信局域网 |

不采用 WebSocket。当前数据量和 1 Hz 刷新频率下，HTTP 轮询更简单、故障恢复更直接，也已在固件中验证。

### 7.2 快照元数据

每个响应必须包含：

- `schema`：协议版本；
- `sequence`：每个完整发布周期递增一次；
- `generated_at`：完整快照生成时间；
- `system`：电脑指标；
- `codex`：Codex 指标；
- `environment`：天气和环境指标；
- `availability`：逐字段可用状态。

桌面端每秒只发布一次完整快照。后台硬件线程只更新锁内待发布值；1 Hz 定时器读取网络和 Codex 后，在同一临界区带入最新硬件值，统一递增 `sequence` 并更新 `generated_at`。API 始终读取最后一个完整的不可变快照。

### 7.3 新旧字段兼容

完整字段表、availability 覆盖规则和错误处理见 [`docs/PROTOCOL.md`](docs/PROTOCOL.md)。

- schema 1 内新增字段必须允许旧固件忽略；
- 新固件读取可选字段时，字段缺失或 `null` 应转为不可用；
- `weekly_remaining` 仅作为旧固件兼容别名，新逻辑使用 `main_weekly_remaining`；
- 当字段语义发生变化或必填结构不兼容时提升 schema；
- PC 与固件必须共享协议测试样例，避免一端已发送而另一端未解析。

### 7.4 新鲜度和离线

- HTTP 连续 5 秒无有效响应：`source_online = false`，保留最后一次有效快照；
- 单字段采集失败：只改变该字段 availability，不把整个设备标记为离线；
- 单字段允许短时间保留最后有效值，但超过保留窗口后必须变为不可用；
- Codex 不活跃与 PC HTTP Link Down 是两个独立状态；
- UI 对不可用值统一显示 `--`，不得显示 0、NaN 或无穷值。

## 8. Codex 指标采集原理

### 8.1 数据来源

当前没有使用远程 API。任务级指标由桌面端只读访问：

- `%CODEX_HOME%/sessions/**/*.jsonl`；
- `%CODEX_HOME%/session_index.jsonl`；
- `%CODEX_HOME%` 未设置时使用 `%USERPROFILE%/.codex`。

账户生命周期累计 Token 不从任务 JSONL 相加，而是启动 Codex 桌面版随附的第一方 `codex.exe app-server --stdio`，完成 `initialize` 后调用 `account/usage/read`。该调用复用 Codex 桌面版已有登录状态，Solis Monitor 不读取认证文件或 Token。

### 8.2 会话选择

1. 读取每个会话文件第一行 `session_meta`；
2. 提取会话 ID、工作目录 `cwd` 和来源；
3. 排除来源中包含 `subagent` 的会话；
4. 按文件最后写入时间排序，选择最新主会话；
5. 从 `session_index.jsonl` 通过 ID 查找 `thread_name`；
6. `TaskName()` 优先返回对应的 `thread_name`，没有标题时回退到项目名，再无可用值时返回 `Codex`；
7. 项目名直接取 `cwd` 最后一级目录，和任务名分别保存；
8. 从最后活动会话最新的 `turn_context` 读取模型和推理强度；
9. 快照与设备协议分别发布 `project`、`task`、`model` 和 `reasoning_effort`。

### 8.3 上下文计算

采集器只解析 `payload.type == "token_count"` 的事件：

```text
context_used_k       = last_token_usage.input_tokens / 1000
context_window_k     = model_context_window / 1000
context_used_percent = input_tokens / model_context_window × 100
```

需要注意：该结果代表 Codex 事件中报告的输入上下文占用，不是对对话文本自行分词估算。

页面“账户累计 TOKEN”使用 `account/usage/read` 返回的 `lifetimeTokens`。它代表账户生命周期累计用量，不是当前任务的 `total_token_usage.total_tokens`，也不是 Solis Monitor 扫描所有任务后自行求和。

### 8.4 周额度计算

- 读取 `rate_limits.primary` 和 `rate_limits.secondary`；
- 只选择 `window_minutes` 位于 `10080±1` 分钟的七天窗口，排除 300 分钟等短窗口；
- `remaining_percent = 100 - used_percent`；
- 显式映射 `codex` 为主额度、`codex_bengalfox` 为 Spark 额度，未知未来 ID 按 `primary` / `secondary` 槽位降级，不要求名称包含 `codex`；
- 显示名称固定为 `主周额度` 和 `GPT-5.3-Codex-Spark`；
- `resets_at` 兼容 Unix 秒、Unix 毫秒、数字字符串和 ISO 时间，并转换为本地时间显示；
- 从所有会话保留每类额度时间最新的读数，避免项目切换后额度倒退。

完整采集、隐私和兼容规则见 `docs/CODEX_METRICS.md`。

### 8.5 刷新、隐私和风险

- 桌面端每秒请求采集器读数，采集器最多每 5 秒扫描一次文件变化；
- 当前把项目、任务、模型、推理强度、上下文数字、账户生命周期累计 Token、两类额度数字、额度名称和重置时间写入设备快照；
- 不发送对话正文、认证信息或 Codex Token；
- 实现会逐行读取会话文件以查找 `token_count`，因此不能宣称完全没有读取正文所在文件；
- 会话 JSONL 是 Codex 桌面版内部格式，不属于稳定公开 API；
- 解析失败记录不含敏感内容的稳定错误类别；可选额度结构损坏时继续发布仍有效的上下文，必要计数字段不可用时显示 Offline；
- 首次扫描缓存会话首行元数据，后续按每个文件的已读字节偏移处理追加内容；文件截断时从头恢复；
- 账户累计 Token 在后台读取，成功后每 6 小时刷新；失败后 5 分钟重试，不能阻塞 1 Hz 指标快照；
- Codex 更新后应通过真实样本兼容测试确认字段结构和额度分类。

## 9. 小屏 UI 设计

### 9.1 通用约束

- 逻辑画布固定为 800×480；
- 使用预生成 RGB565 背景、位图字体和局部动态区域；
- 静态装饰进入背景图，动态数字由固件绘制；
- 字体和图标优先保证 3.97 英寸屏幕的可读性；
- 指标不可用统一显示 `--`；
- 本机 Wi-Fi Link、Codex 活跃状态和字段 availability 使用不同视觉语义；
- 动态文本必须限制长度并按 UTF-8 字符边界截断。

### 9.2 电脑页

- 顶部：下载/上传速度，以及 ESP32 本机 Wi-Fi 图标和 SSID；断线时显示断连图标和“无连接”；
- 上半区：缩小后的 CPU/GPU 双卡；
- CPU：图标、名称、占用率、温度、主频和功耗；
- GPU：图标、名称、占用率、核心温度/主频/功耗、显存已用/总量/占用率和显存温度；
- 下半区：内存卡和物理硬盘卡；
- 内存：图标、已用/总量/占用率和温度；
- 物理硬盘：每行显示名称、占用率和温度，最多 4 块同时显示；能够同时放下时不轮换；
- Windows 活跃网卡名称、FPS 和 PC Online 状态仍保留在指标模型中，但不在当前电脑页顶部显示。

### 9.3 Codex 与环境页

- 顶部：温湿度图标、DHT11 室内温度/湿度和 Codex 活跃/不活跃状态；
- 上半区左侧上下文卡：最后活动项目名、模型、推理强度、上下文已用 K / 总 K、百分比和进度条；
- 上半区右侧周额度卡：主周额度与 GPT-5.3-Codex-Spark 周额度各自的剩余百分比、重置时间和进度条；
- 下半区：位置图标与位置名称、天气图标/温度范围/风向风力，以及累计 Token 数；
- 项目名与模型按 UTF-8 字符边界截断，不轮换；任务名保留在协议中但当前小屏不显示；
- 天气或 DHT11 没有有效读数时显示 `--`，不能显示全零占位数据。

## 10. 桌面端设计

### 10.1 复用边界

保留 Libre Hardware Monitor 的：

- 硬件访问和传感器枚举；
- 原始传感器树、图表、日志和桌面小工具；
- 托盘与开机启动能力；
- 原有 8085 Web 服务；
- 上游许可证和版权信息。

Solis 业务代码集中放在 `Solis/`，不复制一套 LHM 硬件采集内核。

### 10.2 新外壳

目标程序名称为 Solis Monitor。普通模式采用 Windows 11 设置式布局，跟随 Windows 深浅色主题和系统强调色，不显示 CPU、GPU、内存、硬盘、网络、Codex 或天气数据看板。

左侧普通导航固定为四个页面：

1. **设备**：自动发现、配对状态、设备名、IP、Wi-Fi 信号、固件版本、亮度、夜间时段、远程重启和设备 WebUI 入口；
2. **服务**：分别显示设备 API、Codex 和天气 API 状态；提供天气配置、重启 PC 服务和复制诊断信息；
3. **启动与托盘**：只显示“静默启动”和“开机启动”两个独立开关；
4. **固件更新**：选择本地固件执行局域网 OTA，不提供在线 Release 检查和下载。

开发者模式作为隐藏的次级入口保留完整传感器树、原始 Web 服务、日志和模拟数据，不出现在托盘菜单和普通导航中。侧栏默认只显示版本号；在 10 秒内连续点击版本号 10 次后才显示开发者入口。解锁状态跨重启保留，用户可在独立开发者页面主动关闭入口。原始 LHM 功能隐藏但不删除。

服务页底部另设红色危险操作区“恢复默认设置”，不混入四个普通服务模块。二次确认后清除配对、天气、开机启动、静默启动、计划任务、开发者解锁和其他 Solis 用户配置并重新启动；程序、固件、日志和上游传感器配置保持不变。

PC 对外品牌图标采用深灰圆角底板、红色副屏轮廓和屏幕内部的金色太阳；EXE、托盘、标题栏和通知助手使用同一多分辨率图标。对外主程序和程序集使用 `SolisMonitor`，内部源码目录、命名空间、硬件库文件名与上游版权继续保留 `LibreHardwareMonitor`。

### 10.3 设备向导与配对

程序不自动弹出全局首次引导。设备页以“设备向导”作为唯一入口：

1. 副屏尚未联网时，提示长按 GPIO21，通过 AP WebUI 完成 Wi-Fi 连接；
2. 副屏已联网时，提示双击 GPIO21 进入“开启发现”，PC 只列出处于发现状态的设备；
3. 用户选择设备并输入小屏显示的 6 位配对码，PC 自动同步当前 Windows IPv4、固定端口 `18472` 和内部设备令牌；
4. 天气配置在“服务”页维护，开机启动和静默启动在“启动与托盘”页维护，不在设备向导中重复。

配对码每 60 秒刷新，上一组码保留 10 秒宽限期；发现和配对成功页面均保持到用户单击退出。设备令牌只用于认证 ESP32 对 PC 指标 API 的访问，是内部机制，不要求用户查看或输入。令牌失配或用户清除 PC 本地配对后，必须让副屏重新进入发现模式并用 6 位码配对。

### 10.4 托盘、启动与通知

- 手动双击程序始终打开设备控制台；“静默启动”只影响 Windows 开机自动启动；
- 开机启动使用“最高权限”计划任务，首次启用时请求一次管理员授权，之后登录时静默管理员启动；
- 关闭与最小化窗口固定隐藏到托盘，不再提供对应设置开关；
- 托盘菜单只包含小屏状态、打开设备控制台、打开小屏 WebUI、可勾选的“静默启动”和“开机启动”以及退出；
- 小屏连续离线 2 分钟后通知，恢复连接不弹通知；
- 令牌不匹配时通知并引导进入设备向导，重新使用 6 位码配对；
- 天气 API 明确返回 Key、权限或 Host 错误时立即通知；网络超时或 DNS 失败连续 30 分钟后通知；
- 点击通知直接打开“服务”页并显示对应服务状态。

### 10.5 发布与更新

- 开发阶段继续使用便携目录运行；
- 稳定发布提供正式安装包，配置保存在当前 Windows 用户目录，升级不丢失；
- PC 程序和固件均不提供 GitHub Release 在线检查、下载或自动更新；
- 固件 OTA 只接受用户手工选择的本地固件文件；
- OTA 必须校验设备型号、版本和文件完整性，并使用 ESP-IDF 双分区、启动确认和失败回滚能力。

### 10.6 安全边界

- 设备 API 默认监听 18472，通过 Bearer Token 认证；
- 令牌为 64 个十六进制字符（32 字节、256 位随机量），比较时使用恒定时间逻辑；
- 普通界面不显示完整令牌；配对时自动生成和同步；
- 非敏感配置保存在 `%LocalAppData%/SolisMonitor/`，API Key 等秘密使用当前 Windows 用户保护的方式保存；
- 管理操作仅允许本机执行；
- 防火墙只在受信任的专用网络放行；
- HTTP 不提供互联网暴露能力。

## 11. 固件设计

### 11.1 启动流程

1. 初始化板级 GPIO、按键和背光；
2. 检查 PSRAM，失败时停止启动；
3. 初始化 NT35510 和完整帧缓冲；
4. 绘制初始页面并开启背光；
5. 初始化 NVS 和网络配置；
6. 启动 HTTP 指标客户端；
7. 启动串口配置入口；
8. 启动 DHT11 采集任务；
9. 主循环处理按键切页、1 Hz UI 更新和内存水位日志。

目标电源与背光行为：

- 小屏完成部署后只依赖供电和局域网，不依赖 USB 进行日常配置；
- 默认夜间时段为 `23:30–07:30`，设备按本地时间独立关闭背光；
- 夜间首次按键只唤醒背光，唤醒后的按键继续切换页面；30 秒无后续操作后再次关闭背光；
- PC 连续失联 5 分钟后关闭背光，恢复连接后自动点亮；夜间时段仍以夜间规则为准；
- 页面顺序和自动轮换不提供 PC 配置，继续由固件固定顺序和物理按键控制。

### 11.2 组件职责

| 组件 | 职责 |
| --- | --- |
| `board` | 引脚、背光、按键和板级抽象 |
| `display` | NT35510 初始化和矩形刷新 |
| `renderer` | RGB565 图元、文字和进度条 |
| `ui_assets` | 背景与字体二进制资源 |
| `ui` | 页面布局、动态区域和显示规则 |
| `metrics` | JSON 协议、状态仓库和元数据 |
| `network_config` | Wi-Fi/API 配置与 NVS 持久化 |
| `network_client` | Wi-Fi 连接、HTTP 请求、重试和新鲜度 |
| `serial_setup` | 串口交互式配置 |
| `environment` | GPIO47 DHT11 低频采集、校验、失败清除和自动恢复 |

## 12. 实施状态与维护入口

当前任务、已知缺陷、环境阻塞、实施顺序和验收条件统一维护在 [`TODO.md`](TODO.md)。本文只维护稳定设计；任务完成或设计改变时，应在同一提交中更新对应代码、测试、`TODO.md` 和必要的设计说明。已经完成的过程由 Git 历史保存，不在 TODO 中长期堆积。

## 13. 测试与验收

### 13.1 自动验证

- Python：资源生成、RGB565 字节序、屏幕资源尺寸和项目结构；
- .NET：硬件映射、GPU/NVMe 选择、出口网卡、网速计算、Codex 解析、快照和设备 API；
- ESP-IDF：配置存储、JSON 协议、状态清理、网络超时、按键、渲染和 UI 动态区域；
- 构建：PC Release x64、固件 build/size、双 `0x3E0000` OTA 槽容量检查；
- Git：`git diff --check` 和无意外生成文件检查。

### 13.2 实机验收

- COM4 下载、启动和日志；
- PSRAM 容量与最小剩余内存；
- 横屏方向、RGB 顺序、像素字节序、四角与右边界；
- GPIO21 按键切页；
- 1 Hz 更新和连续运行稳定性；
- HTTP 断开 5 秒后 Link Down，恢复后自动上线；
- CPU/GPU/内存/NVMe/网络与 PC 端数据交叉核对；
- Codex 项目切换、上下文 K、主额度、Spark 额度和重置时间；
- 不可用传感器显示 `--`；
- DHT11 和天气失败互不影响其他指标。

## 14. 关键设计决策

| 决策 | 结论 | 原因 |
| --- | --- | --- |
| PC 硬件采集 | 复用 Libre Hardware Monitor | 成熟、开源、已能读取目标硬件，不重复造轮子 |
| 桌面端形态 | 在 LHM 内增加 Solis 业务层和新外壳 | 保留上游功能并共享进程内对象模型 |
| 桌面端普通界面 | 单设备控制中心，不做小屏数据看板 | PC 端承担配置、配对、诊断和更新等独立职责 |
| 设备发现 | 设备页以“设备向导”为唯一入口，在向导内部发现已开启发现模式的设备并用 6 位配对码配对 | 避免“设备向导”和“发现设备”两个相近入口，同时保留明确的物理授权边界 |
| 设备令牌 | 物理授权后自动配对和同步，普通界面隐藏 | 保留局域网认证且不让用户手工管理 64 字符令牌 |
| 固件维护 | ESP-IDF 双分区 OTA，只接受本地固件文件 | 设备长期只供电时仍可升级并支持失败回滚，同时避免在线更新服务的维护成本 |
| ESP32 UI | 自绘 RGB565，不使用 LVGL | 降低资源和依赖复杂度，界面固定且动态区域有限 |
| PC—ESP32 通信 | HTTP 1 Hz 轮询 | 已验证、实现简单、断线恢复清晰 |
| WebSocket | 不采用 | 当前数据量不需要持续连接复杂度 |
| DHT11 来源 | ESP32 本地采集 | 传感器直接连接设备，避免无意义绕行 |
| Codex 来源 | 本地会话 JSONL | 当前唯一可获得任务上下文和额度的本地来源，但需接受兼容风险 |
| 旧 Agent | 不恢复 | 当前桌面端已直接复用 LHM，不需要第二套采集服务 |

## 15. 文档体系

根目录只放 `README.md`、`DESIGN.md`、`TODO.md` 和本地忽略的 `DEVLOG.md` 四个 Markdown 文档。专题文档统一放入 `docs/`，只在有真实内容时创建，不创建空壳文件。待创建文档及创建时机统一记录在 `TODO.md`。

| 文档 | 职责 |
| --- | --- |
| `README.md` | 项目简介、快速构建、运行方法和文档入口 |
| `DESIGN.md` | 稳定需求、总体架构、组件边界和设计决策 |
| `TODO.md` | 当前任务、缺陷、阻塞项、实施顺序和验收条件台账 |
| `DEVLOG.md` | 本地踩坑、实验、演进过程和 Agent 维护记录，不提交 Git |
| `docs/HARDWARE.md` | 硬件清单、引脚和电气约束 |
| `docs/LEGACY_REFERENCE.md` | 旧实现中仍有价值的硬件验证参数 |
| `docs/` | 协议、测试、UI、Codex、天气和仓库审计等专题文档 |
| `docs/REPOSITORY_REVIEW.md` | 目录、生成物、过时代码和后续重构的审计依据 |
| `docs/DESKTOP_APP.md` | PC 端具体构建、运行和设备 API 操作 |
| `docs/PC_MIGRATION.md` | PC 端迁移历史和兼容边界 |
| `docs/REQUIREMENTS.md` | 编号化、可验收的稳定功能与非功能需求 |
| `docs/UI.md` | 800×480 副屏布局、字段、状态、素材和刷新规则 |
| `docs/DECISIONS.md` | 已落地的重大架构决策、替代方案和复审条件 |

文档维护规则：

- `README.md` 不重复完整设计，只链接本文和专题文档；
- `DESIGN.md` 记录稳定总体设计，不维护任务状态或逐次调试日志；
- `TODO.md` 是当前任务状态的唯一事实来源，完成过程由 Git 历史保存；
- 专题文档是该主题的详细事实来源；
- `DEVLOG.md` 记录过程，不能作为对外需求或接口契约；
- 代码、协议和行为改变时，同一提交必须更新对应文档；
- 文档中的“已完成”必须有源码、测试或实机结果支撑。
