cmake_minimum_required(VERSION 3.16)
set(EXTRA_COMPONENT_DIRS "../../components")
set(COMPONENTS main unity renderer ui_assets esp_psram board ui metrics network_config serial_setup network_client ota_update device_control)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
option(SOLIS_UNIT_OTA_COMPAT "Use the production project name for rollback-protected OTA testing" OFF)
if(SOLIS_UNIT_OTA_COMPAT)
    project(solis_monitor)
else()
    project(solis_monitor_unit)
endif()
